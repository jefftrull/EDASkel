cmake_minimum_required( VERSION 2.6 )
PROJECT( new_app )
#find_package( Boost 1.42.0 REQUIRED )
#if (Boost_FOUND)
#    message(STATUS "found boost version")
#else(Boost_FOUND)
#    message(STATUS "did not find desired boost version")
#endif(Boost_FOUND)

add_subdirectory( parser )

